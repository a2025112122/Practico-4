#include<stdio.h>
#include<stdlib.h>
int main (){
int num,resto,inv= 0;

printf("Ingrese un numero entero:");
scanf("%d",&num);

num = abs(num);
while(num>0){

    resto=num%10;
    inv= inv*10+resto;
    num= num/ 10;
}
printf("el resultado de invertir los digitos es: %d\n",inv);
return 0;
}
